// Cypress support file — loaded before every spec.
//
// This is where `/percy:integrate` will add `import '@percy/cypress'` to register
// the cy.percySnapshot() command. There is intentionally no Percy here yet — this
// repo's `main` is the "functional tests, no visual testing" starting state.
